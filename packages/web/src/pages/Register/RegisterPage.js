import styled from 'styled-components';

const OuterRegisterPage = styled.div``;

const RegisterPage = () => (
  <OuterRegisterPage>
    <div>RegisterPage</div>
  </OuterRegisterPage>
);

export default RegisterPage;
