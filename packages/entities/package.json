{
  "name": "@example/entities",
  "private": true,
  "version": "1.0.0",
  "license": "ISC",
  "main": "build/index.js",
  "scripts": {
    "postinstall": "yarn build",
    "clear": "rsync -r --delete --ignore-existing src/ build/",
    "build": "babel src --out-dir build && yarn clear",
    "test": "jest"
  },
  "dependencies": {
    "@example/shared": "*",
    "lodash": "^4.17.20",
    "moment": "^2.29.1",
    "moment-timezone": "^0.5.33"
  },
  "devDependencies": {
    "@example/babel": "*"
  }
}
